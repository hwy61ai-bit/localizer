# Session Kickoff — May 27, 2026

## Where you are

You wrapped May 26 with **Day 1 of the 30-day plan fully shipped, /pricing restyled to Warhol, and Localizer living at the root domain.** The site is structurally ready for paying customers. Free tier and launch comms are the next focus.

**At a glance:**
- ✅ Day 1 (Stripe restructure) — complete
- ✅ Days 4–5 (onboarding welcome page) — complete
- ✅ Day 8–9 (landing page redesign) — mostly complete; only video embed + bio callout outstanding
- ⬜ Day 2 (Stripe webhook consolidation) — next up
- ⬜ Day 6 (welcome email) — drafting starts today
- ⬜ Day 11 (first-asset celebration, empty states) — UX polish, queued
- ⬜ Free tier engineering (5 items) — net-new scope, queued

## Yesterday's wins recap

Quick scan to ground yourself:

- ✅ Landing prices live ($29 / $59 / $129)
- ✅ Localizer moved to root /, /labs preserved (noindex'd), other product routes redirected to /coming-soon
- ✅ Stripe Day 1 complete: 6 live prices, LOCALIZER_PRICE_MAP, 7-day trial in checkout code, priceId validation
- ✅ /pricing restyled to Warhol aesthetic
- ✅ Mobile responsive polish: pricing.css consolidated (deleted, no more !important flags), nav CTA hidden at mobile, LOCALIZER wordmark scales correctly on small viewports

Full session detail in `docs/SESSION_LOG.md`.

## Active blockers

Still on Tim (no change from yesterday):
- ⏳ Onboarding video v1 — notes / approval
- ⏳ Welcome email copy review — drafting starts today, so this unblocks via your action
- ⏳ Canned support response copy (Day 7)

If Tim hasn't surfaced in ~48 hours, worth a nudge.

## Today's menu

Ranked by recommended order:

### 1. Welcome email draft (Day 6) — Drew-owned, ~45 min
Drafting this unblocks Tim's review. Fast win, lights up the launch-comms pipeline.

**Reference:** Tim's pricing decision + welcome flow expectations are in `docs/LOCALIZER_PRICING_DECISION_2026-05-23.md`. Tone should match the onboarding video script's tour-manager-to-tour-manager voice (`docs/LOCALIZER_ONBOARDING_VIDEO_SCRIPT.md`).

### 2. Stripe Day 2 — webhook consolidation — half-day session
Two webhooks coexist today: `/api/stripe/webhook` (simpler) and `/api/billing/webhook` (has idempotency + `planFromPriceId` helper). Consolidate to `/api/billing/webhook` and update `planFromPriceId()` to use the new `tierFromPriceId()` helper from `lib/stripe/localizerPrices.ts`.

After consolidating: remove the unused webhook endpoint from Stripe dashboard so it stops firing.

### 3. Vercel production env var swap — ~10 min
Update `STRIPE_SECRET_KEY` in Vercel Environment Variables to the live mode `sk_live_` key. Triggers an auto-deploy. Without this, production checkout is still pointed at test mode while local dev is live.

### 4. Free tier engineering — watermark renderer — half-day session
First of 5 Free tier items. Watermark needs to overlay `localizer.hwy61labs.com` on assets generated by free-tier accounts. The renderer is `lib/clientRender.ts` (PROTECTED — no edits without explicit authorization). Design the watermark layer as an additive pass that doesn't touch protected code.

### 5. Lower-energy fillers — pick if main work blocks
- Pricing page feature list audit (verify Tim's tier definitions match what's displayed)
- Pricing page Free tier card addition
- Onboarding video v2 re-edit (once Tim's notes land)

## Recommended starting sequence

**If your morning is sharp:** #2 (Stripe Day 2). Builds on yesterday's momentum, all context is fresh, you'll close out the Stripe restructure cleanly.

**If you want a soft ramp:** #1 (welcome email draft). Quick win, unblocks Tim, ~45 min and you're back at the keyboard with energy left for #2 or #3.

Recommended pick: **start with #1, transition to #2 once the email is sent to Tim.** Locks in two days' worth of progress before lunch.

## Before you open the editor

```bash
cd ~/localizer && git pull
```

Confirm clean state:
```bash
git status                  # should be clean
git log --oneline -5        # confirm last commit is yesterday's docs update
```

**Close all `hwy61labs.com` browser tabs** before starting `npm run dev` — open production tabs exhaust Supabase rate limits fast.

Then:
```bash
npm run dev
```

## Workflow reminders

- Two-terminal: chat for planning/diffs, Claude Code for file edits
- Plan → diff → approve before any code change. One file per Claude Code prompt.
- After touching `lib/tourrouter/*` or `lib/supabase*`: run `npx tsc --noEmit && npm run build`
- Stop `npm run dev` before Claude Code writes new files (prevents vendor-chunk corruption)
- Single quotes on commit messages containing `!`, `$`, or square brackets (zsh history expansion / globbing)
- End-of-day: update both `docs/SESSION_LOG.md` AND `docs/LAUNCH_PROGRESS.md`, commit together

## Tim status

Last contact + open threads: check Tim's inbox / Slack for video v1 notes, welcome email response (once you send today's draft), canned support response copy. Bump if silent >48 hours.
